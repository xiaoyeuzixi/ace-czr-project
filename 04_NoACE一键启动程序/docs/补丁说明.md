# 一键启动载荷

启动脚本包含两项当前资源版本修复（Unity 2022.3.62f3）：

1. `updatescript_500_forcequit_fix.ab`：处理数据异常强退分支。
2. `il2cppscripts_0_optional_update_fix.ab`：处理资源版本一致时的可选更新提示。

脚本会核验原始/修改载荷的 SHA-256，遇到未知版本直接结束并记录日志。`backups` 保存回滚副本。

资源缓存中若存在旧版本的带哈希 `il2cppscripts_0.dll.ab_u_*`，脚本会记录其哈希并跳过，不影响当前无后缀 Bundle 的部署。
